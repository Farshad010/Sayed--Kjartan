
GitHub will treat the whole thing as plain text because you nested backticks.

---

# ✔ Try this:  
Copy the version below **exactly as-is** into your README.md:

````markdown
```cpp
/*----------------------------------------------------------------------------*/
/*    Braut eins og á mynd (50 cm hvert strik)                               */
/*----------------------------------------------------------------------------*/

// ---- START VEXCODE CONFIGURED DEVICES ----
// Robot Configuration:
// [Name]               [Type]        [Port(s)]
// Drivetrain           drivetrain    1, 10, D
// ---- END VEXCODE CONFIGURED DEVICES ----

#include "vex.h"

using namespace vex;

int main() {
  vexcodeInit();

  Drivetrain.setDriveVelocity(40, percent);
  Drivetrain.setTurnVelocity(30, percent);

  const int SEG = 500;

  Drivetrain.driveFor(forward, SEG, mm);
  Drivetrain.turnFor(right, 90, degrees);
  Drivetrain.driveFor(forward, SEG, mm);

  // ...rest of your code...
}
